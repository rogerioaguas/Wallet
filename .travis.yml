language: android
jdk: oraclejdk8

android:
  components:
    - platform-tools
    - tools
    - build-tools-26.0.1
    - android-26
    - sys-img-x86-google_apis-26

  before_install:
    - mkdir -p "$ANDROID_HOME/licenses"
    - cp ./android-licenses/ "$ANDROID_HOME/licenses/"