package com.example.brunocolombini.wallet.util.enums

import com.example.brunocolombini.wallet.R

enum class BalanceEventType(val type: Int) {
    FIAT(R.string.fiat),
    BTC(R.string.btc),
    BRITAS(R.string.bts)
}
